// Build version - this file is regenerated at build time
// Changes to this file will force a visible update in the UI

export const APP_VERSION = '2.3.0';
export const BUILD_TIME = new Date().toISOString();
export const BUILD_TIMESTAMP = Date.now();

// This will be visible in the UI to verify deployment
export const VERSION_DISPLAY = `v${APP_VERSION} (${BUILD_TIME.split('T')[0]})`;
